2// Profile sections list — populated after Wednesday Anurag call.
// L7 output generator reads this at runtime — never hardcode this list in pipeline code.
module.exports = {
  PROFILE_SECTIONS: [
    'TBC', // Full section list confirmed post-Wednesday call
  ],
};
